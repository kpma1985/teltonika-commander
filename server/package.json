{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "../scripts/setup-env.sh --if-missing && bun --watch --env-file=../.env run src/index.ts",
    "start": "../scripts/start.sh",
    "typecheck": "bun tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "zod": "^3.24.1",
    "serialport": "^12.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
